{% extends 'admin/base.html.twig' %}

{% block title %}View Product{% endblock %}

{% block page_title %}Product Details{% endblock %}

{% block content %}
<div class="max-w-4xl mx-auto">
    <div class="bg-white rounded-xl shadow-lg border border-gray-100 overflow-hidden">
        <div class="px-8 py-6 border-b border-gray-200 flex justify-between items-center">
            <h1 class="text-3xl font-bold text-gray-900">Product Details</h1>
            <div class="flex space-x-3">
                {% if product.createdBy == app.user or is_granted('ROLE_ADMIN') %}
                <a href="{{ path('admin_products_edit', {'id': product.id}) }}" class="bg-cyan-600 hover:bg-cyan-700 text-white px-4 py-2 rounded-lg font-semibold transition-colors">
                    Edit Product
                </a>
                {% endif %}
                {% if is_granted('ROLE_ADMIN') or (is_granted('ROLE_STAFF') and product.createdBy == app.user) %}
                <form method="post" action="{{ path('admin_products_delete', {'id': product.id}) }}" onsubmit="return confirm('Are you sure you want to delete this product?')" data-turbo="false" style="display: inline;">
                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product.id) }}">
                    <button type="submit" class="bg-red-600 hover:bg-red-700 text-white px-4 py-2 rounded-lg font-semibold transition-colors">
                        Delete Product
                    </button>
                </form>
                {% endif %}
                <a href="{{ path('admin_products') }}" class="bg-gray-300 hover:bg-gray-400 text-gray-800 px-4 py-2 rounded-lg font-semibold transition-colors">
                    Back to List
                </a>
            </div>
        </div>

        <div class="px-8 py-6">
            <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
                <div class="space-y-6">
                    <div>
                        <h3 class="text-sm font-medium text-gray-500 uppercase tracking-wider">Product Information</h3>
                        <div class="mt-4 space-y-4">
                            <div>
                                <label class="block text-sm font-medium text-gray-700">Name</label>
                                <p class="mt-1 text-lg text-gray-900">{{ product.name }}</p>
                            </div>
                            <div>
                                <label class="block text-sm font-medium text-gray-700">Category</label>
                                <p class="mt-1 text-lg text-gray-900">
                                    {% if product.category %}
                                        <span class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-blue-100 text-blue-800">{{ product.category }}</span>
                                    {% else %}
                                        <span class="text-gray-500">No category</span>
                                    {% endif %}
                                </p>
                            </div>
                            <div>
                                <label class="block text-sm font-medium text-gray-700">Price</label>
                                <p class="mt-1 text-2xl font-bold text-green-600">₱{{ product.price }}</p>
                            </div>
                        </div>
                    </div>

                    <div>
                        <h3 class="text-sm font-medium text-gray-500 uppercase tracking-wider">Description</h3>
                        <div class="mt-4">
                            {% if product.description %}
                                <p class="text-gray-900 whitespace-pre-line">{{ product.description }}</p>
                            {% else %}
                                <p class="text-gray-500 italic">No description provided</p>
                            {% endif %}
                        </div>
                    </div>
                </div>

                <div class="space-y-6">
                    <div>
                        <h3 class="text-sm font-medium text-gray-500 uppercase tracking-wider">Metadata</h3>
                        <div class="mt-4 space-y-4">
                            <div>
                                <label class="block text-sm font-medium text-gray-700">Product ID</label>
                                <p class="mt-1 text-lg text-gray-900">{{ product.id }}</p>
                            </div>
                            <div>
                                <label class="block text-sm font-medium text-gray-700">Created By</label>
                                <p class="mt-1 text-lg text-gray-900">{{ product.createdBy.email }}</p>
                            </div>
                            <div>
                                <label class="block text-sm font-medium text-gray-700">Created At</label>
                                <p class="mt-1 text-lg text-gray-900">{{ product.createdAt|date('M j, Y g:i A') }}</p>
                            </div>
                            <div>
                                <label class="block text-sm font-medium text-gray-700">Last Updated</label>
                                <p class="mt-1 text-lg text-gray-900">{{ product.updatedAt|date('M j, Y g:i A') }}</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}