{% extends 'base.html.twig' %}

{% block title %}User Dashboard{% endblock %}

{% block body %}
<div class="min-h-screen bg-gradient-to-br from-cyan-50 via-white to-blue-50">
    <!-- Navigation -->
    <nav id="navbar" class="bg-cyan-700 shadow-lg fixed top-0 left-0 right-0 z-50 transition-transform duration-300">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between h-16">
                <div class="flex items-center">
                    <div class="flex-shrink-0 flex items-center space-x-2">
                        <img src="/images/densyst-logo.png" alt="DenSyst Logo" class="h-10 w-auto">
                        <span class="text-white text-xl font-bold">Dashboard</span>
                    </div>
                </div>
                <div class="flex items-center space-x-4">
                    <span class="text-white">Welcome, {{ app.user.userIdentifier }}</span>
                    <a href="{{ path('app_logout') }}" class="bg-red-600 hover:bg-red-700 text-white px-4 py-2 rounded-md text-sm font-medium transition-colors">Logout</a>
                </div>
            </div>
        </div>
    </nav>

    <!-- Hero Section -->
    <section class="relative pt-32 pb-16 px-4 sm:px-6 lg:px-8">
        <div class="max-w-7xl mx-auto">
            <div class="text-center animate-fade-in-up">
                <h1 class="text-4xl md:text-5xl font-extrabold text-gray-900 mb-4">
                    Welcome to Your
                    <span class="bg-gradient-to-r from-cyan-600 to-teal-600 bg-clip-text text-transparent">Dashboard</span>
                </h1>
                <p class="text-lg text-gray-600 max-w-2xl mx-auto">
                    Manage your appointments, view treatment history, and access all your dental care information in one place.
                </p>
            </div>
        </div>
    </section>

    <!-- Dashboard Content -->
    <section class="pb-16 px-4 sm:px-6 lg:px-8">
        <div class="max-w-7xl mx-auto">
            <!-- Stats Cards -->
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 mb-12">
                <!-- Appointments -->
                <div class="bg-white/80 backdrop-blur-sm p-8 rounded-2xl shadow-lg hover:shadow-2xl hover:-translate-y-2 transition-all duration-500 animate-fade-in-up border border-cyan-100">
                    <div class="flex items-center mb-6">
                        <div class="w-16 h-16 bg-gradient-to-br from-cyan-500 to-blue-600 rounded-2xl flex items-center justify-center shadow-lg">
                            📅
                        </div>
                        <div class="ml-6">
                            <h3 class="text-2xl font-bold text-gray-900">3</h3>
                            <p class="text-gray-600">My Appointments</p>
                        </div>
                    </div>
                    <a href="#" class="text-cyan-600 hover:text-cyan-700 font-semibold transition-colors">View appointments →</a>
                </div>

                <!-- Treatments -->
                <div class="bg-white/80 backdrop-blur-sm p-8 rounded-2xl shadow-lg hover:shadow-2xl hover:-translate-y-2 transition-all duration-500 animate-fade-in-up delay-200 border border-cyan-100">
                    <div class="flex items-center mb-6">
                        <div class="w-16 h-16 bg-gradient-to-br from-teal-500 to-cyan-600 rounded-2xl flex items-center justify-center shadow-lg">
                            🦷
                        </div>
                        <div class="ml-6">
                            <h3 class="text-2xl font-bold text-gray-900">12</h3>
                            <p class="text-gray-600">Treatment History</p>
                        </div>
                    </div>
                    <a href="#" class="text-cyan-600 hover:text-cyan-700 font-semibold transition-colors">View history →</a>
                </div>

                <!-- Profile -->
                <div class="bg-white/80 backdrop-blur-sm p-8 rounded-2xl shadow-lg hover:shadow-2xl hover:-translate-y-2 transition-all duration-500 animate-fade-in-up delay-400 border border-cyan-100">
                    <div class="flex items-center mb-6">
                        <div class="w-16 h-16 bg-gradient-to-br from-blue-500 to-indigo-600 rounded-2xl flex items-center justify-center shadow-lg">
                            👤
                        </div>
                        <div class="ml-6">
                            <h3 class="text-2xl font-bold text-gray-900">100%</h3>
                            <p class="text-gray-600">Profile Complete</p>
                        </div>
                    </div>
                    <a href="#" class="text-cyan-600 hover:text-cyan-700 font-semibold transition-colors">Update profile →</a>
                </div>
            </div>

            <!-- Quick Actions -->
            <div class="bg-white/80 backdrop-blur-sm rounded-3xl p-8 shadow-2xl border border-cyan-100">
                <h2 class="text-3xl font-bold text-gray-900 mb-8 text-center">Quick Actions</h2>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                    <a href="#" class="bg-gradient-to-r from-cyan-600 to-teal-600 hover:from-cyan-700 hover:to-teal-700 text-white p-6 rounded-2xl shadow-lg hover:shadow-xl transform hover:scale-105 transition-all duration-300">
                        <div class="flex items-center">
                            <div class="w-12 h-12 bg-white bg-opacity-20 rounded-xl flex items-center justify-center mr-4">
                                📅
                            </div>
                            <div>
                                <h3 class="text-xl font-bold">Book Appointment</h3>
                                <p class="text-cyan-100">Schedule a new visit with ease</p>
                            </div>
                        </div>
                    </a>
                    <a href="#" class="bg-gradient-to-r from-teal-600 to-cyan-600 hover:from-teal-700 hover:to-cyan-700 text-white p-6 rounded-2xl shadow-lg hover:shadow-xl transform hover:scale-105 transition-all duration-300">
                        <div class="flex items-center">
                            <div class="w-12 h-12 bg-white bg-opacity-20 rounded-xl flex items-center justify-center mr-4">
                                💬
                            </div>
                            <div>
                                <h3 class="text-xl font-bold">Contact Support</h3>
                                <p class="text-teal-100">Get help from our dental team</p>
                            </div>
                        </div>
                    </a>
                </div>
            </div>
        </div>
    </section>
</div>
{% endblock %}